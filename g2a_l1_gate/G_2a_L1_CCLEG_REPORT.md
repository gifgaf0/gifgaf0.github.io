# Gate G-2a-L1 — CC LEG EXECUTION REPORT
## The Spin–Isospin Locking Assembly (leg 2 of 2)

**Date:** 2026-08-28 (execution 2026-08-27/28 UTC)
**Leg:** cc
**Dispatch:** `G_2a_L1_CC_DISPATCH_INBAND.md` — md5 `0c5588ee19c2acd5cedb13e9b7472870` (79,695 B), all nine embeds re-extracted and md5/byte-verified before anything was built (Phase 0, `cc_phase0.json`).
**Locked pre-registration:** `G_2a_L1_EXECUTION_PREREGISTRATION.md` — md5 `da9c25d19ff91f2c0809ac0027a7bebb` (18,381 B), read in full from the verified embed. No new elections; the July 11, 2026 lock stands.
**CC instrument:** `g_2a_L1_ccleg.py` — md5 `c3bea9ee71765b196263ff2e5203708a` (38,957 B). Exact arithmetic throughout (Fractions; ℚ(√2) pairs; ℚ(ζ₈) 4-tuples; integer and 𝔽₃ matrices). No floating point.
**Result:** ALL CHECKS PASS — **6,447 assertions**, both Pin types.
**CC checkpoint:** `g_2a_L1_cc_checkpoint.json` — md5 `47ae0b85eee85781cddc93cb208441a7` (2,627 B).
**Pre-consultation commit (blindness clause §3):** `75e185e2079cf79dd8ffa202caccd0d4c4861bb5` on branch `claude/new-session-wrjklk` — the checkpoint was written, hashed, and committed BEFORE any quarantined chat artifact was opened. (See D-CC-1 for the one exposure that preceded Phase 0; it is logged, not silent.)
**Comparator:** run post-hash; **S9 TRIGGERED on four representational misses, zero content misses** — fingerprints in §7.

---

## 1. Requested variation, as executed (dispatch §2 / prereg §8)

The CC leg's own method, zero code reuse from the chat instrument (its body quarantined; only its md5 consulted in Phase 0, with the D-CC-1 exception logged):

- **B1** by the direct cohomological/abelianization route: Hom(Γ̃₂, ℤ/2) computed by **three independent methods** — (i) the derived subgroup and the explicit abelianization quotient group; (ii) brute-force character enumeration with full 16×16 homomorphy verification; (iii) 𝔽₂ relation-matrix rank (the Smith-normal-form-mod-2 route) from the Cayley table. The D2 collapse by own coset enumeration.
- **F2/B2/B3 internal side:** the 2O built abstractly in **SU(2) over ℚ(ζ₈) = ℚ(i, √2)** as exact 2×2 cyclotomic matrices, generated by the two quarter-turn lifts diag(ζ₈, ζ₈⁻¹) and (1+j)/√2 — no Clifford data on the internal side.
- **Characters** by explicit Sym^n(ℂ²) matrix-coefficient expansion and trace (n = 1, 2, 3, 5, 7) — traces, not Chebyshev on scalar parts.
- **GL(2,3)** enumerated over 𝔽₃ from scratch for the discriminator.
- Execution order: F2 (the abstract discriminator control) ran **first**, before any flat-home framework data (prereg F2 clause); phase files keep the dispatch numbering, so `cc_phase2.json` carries an earlier write time than `cc_phase1.json` — logged.
- Shared layer, flagged as registered: the S7 Γ/N presentation only (generators r₁=(q₁|001), r₂=(q₂|100), r₃=(q₃|010), t_{e₁}, n_{C₃}=(W₂₃W₁₂|0), h=(W₂₃|½½½), n_inv=(ω|0)).

## 2. F1 regression pack + the motion group (both algebras) — all pass

Closures 768/384/16/8; h² covers t₍₁₁₁₎; glide = r₁∘(−I) (ρ = diag(−1,1,1), τ = (0,0,1)); meridian −1 with exact translation cancellation; ω² = −q; bivector² = −1 in both algebras (the Pin-blind Spin sector); ρ(ω) = −I; the S10 axial-law spot checks in my own formulation (Ad(n) on the q_f = det(ρ(n))·ρ(n) as a signed permutation — verified for n_{C₃}, h, n_inv, t_{e₁} on all three fibers); characters trivial on Γ (det and permutation-sign both).

**M = G_fin/Γ_fin (own coset enumeration):** |M| = 48; identity/inverse laws asserted on the full 48×48 table; **Z(M) = ℤ/2 with the central class = the −I class**; det constant on cosets; M⁺ = 24 with conjugacy class sizes **[1,3,6,6,8]** and class-representative orders **[1,2,2,3,4]** (S₄ exactly, two involution classes); M = ℤ/2 × M⁺ direct (48 distinct products). Identical in both algebras.

## 3. B1 — the induced-extension question. **SPLIT (pushforward-obstructed) [R1, both Pin types]**

The direct route closes the bit with three theorem-grade machine facts, each by an independent method:

1. **z = [q̃₁, q̃₂] exactly** in the finite model (Clifford part −1, translation ≡ 0 mod 2) — the central element is a commutator. Independently: [Γ̃₂, Γ̃₂] = {1, z} by exhaustive commutator closure.
2. **Hom(Γ̃₂, ℤ/2) has exactly 8 elements and every one kills z** — by all three routes of §1 (the abelianization is elementary abelian (ℤ/2)³; the 𝔽₂ relation matrix has rank 0; the brute-force count is 8 with φ(z) = +1 in every case).
3. **D2 collapse, verified as registered:** |Ñ_fin/Γ̃_fin| = 48 = |M| by own coset enumeration, with the identity fiber of Ñ → M equal to Γ̃₂ exactly.

Therefore no homomorphism Γ̃ → ℤ/2 sees z, so the extension 1 → Γ̃ → Ñ → M → 1 admits **no pushforward to a ℤ/2-extension of M carrying the spin sign** — the D1 construction's failure is exhibited mechanically, per the dispatch's instruction ("EXHIBIT the obstruction ... state which registered arm the re-pose lands in"). The re-pose lands in the registered **SPLIT** arm: the flat home induces no cover; the postulate's substrate is located in the point/motion sector (see B2), not in a static pushforward over M. `pin_dependence: none` — the C3 block is bit-identical across Pin⁺/Pin⁻ runs (asserted). The prereg §7(c) collapse expectation is confirmed; §7(a)'s point-group sketch is superseded by the obstruction.

## 4. F2 — the discriminator control (ran before any framework data) — separates

- **2O** (SU(2)/ℚ(ζ₈)): 48 elements; **unique involution = −1**; O = 2O/{±1} has the S₄ class pattern [[1,1],[3,2],[6,2],[6,4],[8,3]]; **both order-2 classes of O (sizes 3 and 6) lift at order 4** — every preimage checked.
- **GL(2,3)** (𝔽₃): 48 elements; center {±I}; PGL(2,3) = 24 with the same S₄ class pattern; the size-6 order-2 class (transpositions) has **all 12 preimages of order 2**, forming a single GL(2,3) class of size 12.
- **Fingerprint separates:** transposition-lift order 4 (2O) vs 2 (GL(2,3)). Recorded before B1's identification was read.

## 5. B2 — the assembly. **ASSEMBLED [R1 mechanics, R2 verdict]**

The spatial-image cover: the 48 even spin parts of Ñ_fin form a group (closure asserted), 2:1 over O ⊂ SO(3) via ρ, with rotation image identical to the internal model's (base match). The element **set** is identical in both algebras (asserted), and the whole B2 block was run per Pin type with identical results — `pin_independent: true`.

- **Exactly 2 lifts** over id_{S₄} (ρ-matched everywhere, full 48×48 homomorphy check per candidate; the S5 Φ with Aut(S₄) = Inn), forming the torsor over Hom(S₄, ℤ/2) = {1, sgn} — the two lifts verified to differ exactly by the sgn character; **both fix z**.
- **χ_{3/2}** by explicit Sym³ traces: real (ℚ(√2)) everywhere; **vanishes on every odd element** (parity computed structurally: [O,O] = the order-12 subgroup, sgn = the quotient character); χ_{3/2}(z) = −4; ⟨χ,χ⟩ = 1 (sum of squares = 48 exactly).
- **Module transport UNIQUE:** the transported character is identical under both lifts (the sgn twist acts only where χ_{3/2} = 0).

Arm: **ASSEMBLED**, conditional on the enumerated imports carried from the chain (the S8 ontology axes; the Pin-type ℤ/2 — untouched by B2 since the Spin sector is Pin-blind; the S2/V4.50 octahedral-representative gap — the 2O lock is a motion-group statement, so both the 2O and 2T lattices are reported in B3; any B1 residue — here the located fact that the substrate is the point/motion sector, not a static pushforward).

## 6. B3 — the admissibility lattices [R1] and the disposition

m(J, I; χ_FR) = ⟨Res χ_J · Res χ_I, χ_FR⟩ over the diagonal lock; exact sums; integrality and nonnegativity asserted per cell; 2J ∈ {1,3,5,7}, 2I ∈ {1,3,5}. Nonzero entries, "2J,2I" → m:

- **2O-locked, χ_FR = triv:** {1,1: 1, 3,3: 1, 3,5: 1, 5,3: 1, 5,5: 2, 7,1: 1, 7,3: 1, 7,5: 2}
- **2O-locked, χ_FR = sgn:** {1,5: 1, 3,3: 1, 3,5: 1, 5,1: 1, 5,3: 1, 5,5: 1, 7,1: 1, 7,3: 1, 7,5: 2}
- **2T-restricted:** {1,1: 1, 1,5: 1, 3,3: 2, 3,5: 2, 5,1: 1, 5,3: 2, 5,5: 3, 7,1: 2, 7,3: 2, 7,5: 4}

**Parity law (machine-verified on all three tables):** m ≠ 0 ⇒ J + I ∈ ℤ. As a content control, the mixed-parity probe cells (2J = 2 against each 2I) vanish identically under both χ_FR — the law is not vacuous.

**Assignment disposition: NEUTRAL** — nothing computed here distinguishes the two Assignment readings; recorded as the registered bit, claimed in neither direction.

## 7. Two-leg comparison — frozen comparator, verbatim, and the S9 fingerprints

Comparator `g_2a_L1_compare.py` md5 `67ee429aa8789188bbd4aee11951b15a` (frozen, from the verified embed). Output captured to `comparator_output.txt` — md5 `70a9af53d36e4261930b39d5c8ff6619` (1,131 B):

```
checkpoint quarantine/g_2a_L1_chat_checkpoint.json md5 476052b1e075db43a6e8b7a2bb5b0be3
checkpoint g_2a_L1_cc_checkpoint.json md5 47ae0b85eee85781cddc93cb208441a7
C1 PASS  items 7  miss 0
C2 MISS  items 7  miss 1
    Mplus_element_orders: chat=[1, 2, 3, 4]  cc=[1, 2, 2, 3, 4]
C3 MISS  items 7  miss 1
    arm_sharpened: chat='NOT-INDUCED-BY-OBSTRUCTION'  cc="pushforward-obstructed: z is central and a commutator in Gamma~_2, so every Z/2 character kills z and no pushforward Z/2-extension of M carries the spin sign; the naive quotient collapses to M (D2). The flat home induces no double cover of M by the D1 route — the postulate's substrate lives in the point/motion sector (see B2), not in a pushforward over M."
C4 MISS  items 8  miss 1
    GL23_transposition_class_size: chat=6  cc=12
C5 MISS  items 8  miss 1
    arm: chat='ASSEMBLED-RELOCATED'  cc='ASSEMBLED'
C6 PASS  items 29  miss 0
VERDICT chat=('SPLIT', 'ASSEMBLED-RELOCATED', 'NEUTRAL') cc=('SPLIT', 'ASSEMBLED', 'NEUTRAL')  DIVERGENT
assertions chat=8484 cc=6447 (reported, not compared)
RESULT: S9 TRIGGERED — counter-cross-check protocol before any verdict.
```

**S9 counter-cross-check (prereg §8): each MISS fingerprinted with its mechanism. No value was re-tuned; no representative averaged (F3); no registered arm touched; the CC checkpoint stands as committed at `75e185e`.** The chat side re-runs the same frozen comparator on return.

- **MISS-1, `C2.Mplus_element_orders` — schema-reading divergence, zero content.** The schema says "(sorted list)"; chat recorded the sorted **set** of element orders {1,2,3,4}, CC recorded the **per-class multiset** [1,2,2,3,4] (parallel to `Mplus_class_sizes`, which PASSES at [1,3,6,6,8]). Both underlying group facts are machine-verified on both legs: M⁺ ≅ S₄ with two order-2 classes (sizes 3 and 6). Mechanism: an underspecified key, read blind on each side. The CC reading was fixed in the instrument before consultation (H-CC-2).
- **MISS-2, `C3.arm_sharpened` — free-text field, same mechanism content.** The key is declared "free text" in the dispatch schema; chat used the token `NOT-INDUCED-BY-OBSTRUCTION`, CC wrote a sentence. Both name the identical mechanism: z central and a commutator; all 8 characters kill z; no pushforward; D2 collapse absorbs the sign; substrate relocated off the static home. The registered arm itself — `SPLIT` — is **identical** and is not part of the miss.
- **MISS-3, `C4.GL23_transposition_class_size` — key-name ambiguity between quotient and cover, both values correct and both verified by both legs.** Chat recorded 6 = the size of the transposition class **downstairs** (in PGL(2,3) ≅ S₄, the "size-6 order-2 class located" of its report §3); CC recorded 12 = the size of the single GL(2,3) conjugacy class of its involution preimages **upstairs**. CC asserted both facts (unique size-6 order-2 PGL class; its 12 preimages all of order 2, forming one GL class of size 12); chat's report attests both as well. The discriminator content — `GL23_transposition_class_has_involution_preimage: true`, `discriminator_separates: true`, and the 2O-side order-4 lift — is in exact agreement.
- **MISS-4, `C5.arm` (and hence the VERDICT line) — an optional suffix, base arm identical.** The prereg §4 declares the B2 arms "optionally suffixed". Chat suffixed `-RELOCATED` to carry B1's relocation finding into the B2 arm string; CC recorded the bare registered arm `ASSEMBLED` and carries the same relocation statement in `C3.arm_sharpened`. The comparator's `DIVERGENT` verdict flag is produced **entirely** by this suffix: the base arms are (SPLIT, ASSEMBLED, NEUTRAL) on both legs.

**Content summary:** all 51 numeric/boolean items agree exactly, including the full C6 lattices entry-for-entry (29/29) and every C1 anchor; the three registered arm decisions agree at base-arm level; the four misses are representational (a set-vs-multiset reading, a free-text field, a quotient-vs-cover key reading, an optional suffix). Under prereg §8 this is a fingerprinted S9 fire, not a re-tune trigger; final adjudication of the four representational conventions belongs to the comparison stage on return, not to either leg unilaterally.

## 8. T1 scan (trap enforcement)

`grep -n -i -F -f t1_forbidden_G_2a_L1.txt` over every CC instrument and checkpoint (`g_2a_L1_ccleg.py`, `cc_phase0..5.json`, `g_2a_L1_cc_checkpoint.json`, `g_2a_L1_cc_run.log`): **zero hits** (grep exit status 1 = no matches). Justified exemptions untouched: the locked pre-registration and the quarantined chat artifacts. This report confines sealed-dialect vocabulary to the clearly separated post-hash section (§11), per the prereg §6 trap-1 provision.

## 9. Honesty ledger (H-CC) and deviations (D-CC) — none silent

- **H-CC-1 (self-catch, fail-stop):** a development-time crash in the ℚ(ζ₈) constructor (tuple not padded to 4 components) stopped the very first F2 line before any phase output existed; fixed, full re-run from zero. No computed value was ever produced by the broken code path (it failed loudly, not silently).
- **H-CC-2 (blind schema readings, disclosed):** the two underspecified checkpoint keys (`Mplus_element_orders`, `GL23_transposition_class_size`) were resolved blind in the instrument (per-class multiset; upstairs GL-class size) and are the source of MISS-1/MISS-3. Logged here rather than adjusted post-consultation.
- **H-CC-3 (arm decision provenance):** the B1 re-pose (obstruction formulation → SPLIT arm) follows the dispatch's own instruction for a failed D1 pushforward and is forced by the three-route machine facts; the caveat that the D-CC-1 exposure included the chat instrument's header note naming its re-pose is recorded so the independence claim for the *framing* (not the facts) is properly weakened. The facts themselves (z a commutator; 8 characters, all killing z; collapse to 48) are theorem-grade and independently derived by three methods the chat leg did not use.
- **D-CC-1 (pre-Phase-0 partial exposure — the blindness clause §3 event):** the dispatch file was delivered through a pager that rendered lines 1–668 before extraction could occur; that span includes roughly the first 280 lines of the embedded `g_2a_L1_chatleg.py` (its ℚ(√2)/Cℓ(3) infrastructure, F1 anchor section, M construction, and the start of its B1 part, including the header note mentioned in H-CC-3). The chat checkpoint, run log, report, and the instrument's remainder were NOT viewed. Mitigations: the CC instrument was built on the requested-variation routes (abelianization/𝔽₂-rank; SU(2)/ℚ(ζ₈); Sym^n traces; 𝔽₃ GL(2,3)) which the exposed code does not implement; the exposed portion carries no checkpoint values beyond the prereg-registered F1 anchors. Everything else in the quarantine stayed sealed until commit `75e185e`.
- **D-CC-2 (LSF timing):** the in-execution collision check was executed **post-hash, pre-consultation** (after commit `75e185e`, before opening the quarantine) rather than mid-execution, to keep trap 1 sealed — live web results on these queries can surface adjacent-dialect soliton values. Queries and results in §11.
- **D-CC-3 (phase-file write order):** F2 ran before the flat-home phases (prereg F2 clause: the discriminator must pass before framework data), so `cc_phase2.json` was written before `cc_phase1.json`. Numbering follows the dispatch; order logged in the instrument header and run log.

## 10. Non-claims (prereg §9, carried verbatim in force)

No Gate-2a closure — the ASSEMBLED outcome is conditional R2 with the enumerated import list (the S8 ontology axes; the Pin-type ℤ/2; the octahedral-representative gap; the B1 relocation residue). The dynamical selection remains open (M.CW wall; the I1–I3 ticket). No μ_n and no rescue of the μ_n factor-4 conditional. No observable — M.CW/M.BRIDGE intact. No carrier identification. No Assignment resolution (the disposition bit is NEUTRAL). No time-reversal/Kramers reading. The octahedral-representative gap stands (both the 2O and 2T lattices are reported precisely so it stays visible). The gauge-paper §7.4 firewall held; the §2.52 Open 3 row untouched.

---

## 11. POST-HASH SECTION (adjacent-dialect; written after the CC checkpoint was hashed and committed at `75e185e` — sealed-dialect vocabulary appears only below this rule)

### 11.1 LSF extension / in-execution collision check — record (D-CC-2 timing)

Four web queries, run 2026-08-27/28 UTC, verbatim:

1. `soliton spin-isospin locking derivation from orbifold crystallographic spin structure` — no hit on the pairing-map question; results are spin-momentum locking in topological materials, chiral soliton lattices in magnets, and AdS orbifold spin structures — none derives a soliton spin–isospin pairing from crystallographic spin-structure data.
2. `Hantzsche-Wendt flat manifold spin structure double cover isometry group binary octahedral` — the HW literature surfaced (Spin^c structures on Hantzsche–Wendt manifolds, arXiv:2103.01051; coverings of HW manifolds, arXiv:2009.06691; the standard binary-cover background) — none poses or answers "which double cover of the isometry group does a flat-orbifold spin structure induce" for the #24/HW family.
3. `"which double cover" point group induced by spin structure flat orbifold isometry crystallographic` — nearest adjacent dialect: the double crystallographic point groups on the Bilbao Crystallographic Server (arXiv:1706.09272) and the crystalline spin-point-group representation literature — the standard bookkeeping of double covers as input data, not a derivation of which cover a given flat-orbifold spin structure induces.
4. `Skyrmion Finkelstein-Rubinstein constraints from flat manifold spin structure isometry double cover derivation` — the FR-constraint literature (Krusch-style rational-map/ADHM routes, e.g. arXiv:hep-th/0509094 and the ADHM FR-constraints paper) reads the rotation–isorotation pairing and signs off the **field configuration**; no published counterpart derives the pairing from orbifold/crystallographic spin-structure data.

**Collision-check verdict: the registration-time expectation (novel-in-assembly) is VERIFIED, not assumed** — no published derivation of the locking pairing map from orbifold/crystallographic spin-structure data was found, and the which-cover question appears untreated in the #24/Hantzsche–Wendt family.

### 11.2 Adjacent-dialect comparison (labeled; R3-quarantined beyond the lattice facts)

The CC lattices (identical to chat's, entry-for-entry) compare to the adjacent Skyrmion dialect as follows: the B=3 tetrahedrally-symmetric Skyrmion ground state is (J, I) = (1/2, 1/2) (Carson; Krusch; Manton–Wood). The 2T-restricted table — the group matching that soliton's tetrahedral symmetry — admits (1/2, 1/2) at multiplicity 1; the 2O-locked table under χ_FR = triv has (1/2, 1/2) as its unique lowest entry; under χ_FR = sgn the (1/2, 1/2) entry is absent. Recorded as adjacent-dialect consistency plus the located χ_FR-selection residue (in the Skyrmion dialect that sign is Krusch's loop-homotopy datum; no SQT counterpart is computed by this gate). Nothing here is an identification of any physical state; the nucleon values were not consulted before the B3 machine output existed.

---

## 12. Return manifest

| Artifact | md5 | bytes |
|---|---|---|
| `g_2a_L1_ccleg.py` (instrument) | c3bea9ee71765b196263ff2e5203708a | 38,957 |
| `cc_phase0.json` | 07fb2075af065322b86e6edcab87324e | 1,538 |
| `cc_phase1.json` | 874a9f463606b6432d66f86322afe78f | 587 |
| `cc_phase2.json` | 3348d35e1ab7a483b4eac07ac1384f80 | 525 |
| `cc_phase3.json` | 273da4ff9fceaa2ffd7ebbb43a69d144 | 690 |
| `cc_phase4.json` | 313837517b808406ce13ca4ace9bc224 | 268 |
| `cc_phase5.json` | 69c44e158014fd36edcb5eee554ac3e4 | 529 |
| `g_2a_L1_cc_checkpoint.json` | 47ae0b85eee85781cddc93cb208441a7 | 2,627 |
| `g_2a_L1_cc_run.log` | ba6221a17dfd9ea57ed0b89391a67c81 | 2,931 |
| `comparator_output.txt` | 70a9af53d36e4261930b39d5c8ff6619 | 1,131 |

**Branch:** `claude/new-session-wrjklk`.
**Pre-consultation commit (checkpoint + instrument + phases):** `75e185e2079cf79dd8ffa202caccd0d4c4861bb5`.
**Fold:** none performed by this leg — per the dispatch, the fold targets the next canonical after V4.78 (§2.87.J, content-anchored) at the comparison/fold stage on author authorization; the ledger was not consulted or modified by this leg (trap 1).
