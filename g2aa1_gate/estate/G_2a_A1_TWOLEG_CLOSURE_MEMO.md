# G-2a-A1 — TWO-LEG CLOSURE MEMO

**Date:** September 21, 2026. **Base:** V4.83 `40009ec0197876b130766f1ae7494360`. **Memo lock:** v2 `626aa868844220eb6c3801ef07a77783` (61,562 B). **Lock record:** `8732c390` (Addendum A-2). **Comparator v1.0** `5afcd589` + **schema v1.0** `53111e03` (frozen pre-emission). **Elections (T3):** E-A1-1(a) · E-A1-2(a) · E-A1-3(a) · E-A1-4(a) · E-A1-5(a) · E-A1-6(a) · E-A1-7(a) · E-A1-8(a). **Author's answers of record:** Q-A1-1 (16 real components; O not U(1)-phase-invariant), Q-A1-2 (the M.ONT texture envelope is a separate component — H2), Q-A1-3 (no real-unit-splitting term; H-A1-5 scoping note kept).

## 1. Verification chain on the CC return (chat-side, bytes not prose)

1. **Retrieval.** Branch `claude/new-session-j6430a` of `gifgaf0/gifgaf0.github.io` (shallow clone; base = `main` at the PR #24 merge `0378056`), directory `g2aa1_gate/`; `G_2a_A1_CC_RETURN_INBAND.md` md5 `6034515b769dedbe296ee39756847850` (302,603 B). The dispatch CC worked from is in the tree as `dispatch.md` = the staged `b2aa9953` byte-for-byte.
2. **Embeds.** Five sentinel blocks re-extracted with the dispatch's own extractor rule, each byte-exact against its declared md5 and its in-tree copy: instrument `006cb5213208e41cc57cfe045b7d3dd4` (76,216 B) · checkpoint `bbf94221122136f00c5ecad0b90e5313` (8,652 B) · two-leg comparison `5942248e4be4b357017c6822817ca4c2` (66,776 B) · chat-vs-chat re-run `ba0876074396e697614a4daa78da7682` (byte-identical to the quarantined chat original) · CC-vs-CC sanity `73a19a11288475ed3eb8935e0a3f9c66`.
3. **Blindness ordering (git).** `8323968` (03:55:24 UTC, September 21) — pre-consultation checkpoint: the tree holds the CC instrument, the checkpoint blob hashing `bbf94221` (quoted in the commit message), the dispatch blob `b2aa9953`, the plain embeds and the T1 tools — **no chat artifact**; the seven armored embeds unopened. `2be2afc` (03:57:10) — first appearance of the decoded chat instrument, checkpoint, compare, log, report and prechecks, plus the two comparison files. `0002154` (03:59:48) — the return. Order verified.
4. **Comparator run of record (chat-side, frozen v1.0).** chat `8f657a23` vs CC `bbf94221`: **436 checks, 430 PASS, 6 MISS** — the chat-side output (`cc/g_2a_a1_twoleg_comparison_CHATSIDE_RUN.json`) is **byte-identical** to CC's own `g_2a_a1_twoleg_comparison.json` (`5942248e` both sides) — comparator determinism confirmed.
5. **Determinism witness (the G-2a-L1 practice).** CC's instrument re-executed chat-side in a directory holding only the four guarded files (memo, gate T1 list, scanner, extract; all md5 guards held; T1 self-scan CLEAN; 2.6 s) regenerates the checkpoint **identically in all 185 leaf values except `extras.elapsed_seconds`** (3.88 → 2.57); the timing-stripped canonical md5 is `42a65c26ae895022b1a349b520437b0a` on both. The frozen comparator on the re-executed checkpoint gives the same 436 / 430 / 6 with the same six rows.
6. **Independence witness.** Instrument md5s differ; checkpoints not byte-identical; CC's instrument is `Fraction`-exact without numpy, builds g₂ as the φ-annihilator in so(7) (then cross-verifies the derivation identity), enumerates SL(2,7) and GL(3,2) from scratch, carries its own exact-sequence module, and reads the branchings from Casimir spectra plus an exact sl₂-triple — every method variation the dispatch requested (CC-DD-1..3 are the definitions it had to fix).
7. **T1.** Gate list `2026b782` (24 patterns) md5-asserted on both legs (never self-scanned); scanner `6b862900`. CLEAN on every artifact both legs — instrument/memo/extract at every invocation; both checkpoints; both comparison files; the return; this memo and the v1.1 candidates below (0 hits, 0 numeric collisions). D-A1-1 (the armor-transport collision inside one dispatch base64 body) stands as recorded; nothing here changes it.

## 2. Two-leg verdict: ASSIGNMENT-I-FORCED on both legs — zero deviation on every verdict-bearing quantity

T1, T2, T3, T4 all hold; the spatial–internal direct product holds; Sym_int pinned; all controls pass; **F-A1-1…F-A1-7 all silent on both legs**; the verdict recomputed from the booleans by the schema rule equals the reported verdict on both legs. Every integer witness agrees exactly and every float rule passes on both legs:

| Witness | chat | CC |
|---|---|---|
| Full continuous stabilizer of the O density in so(16) | 15 = g₂ ⊕ u(1)_{ψ₀} (imaginary sector 14) | 15 = g₂ ⊕ u(1)_{ψ₀} (in `extras`; schema keys "G2"/14) |
| Phase subgroup preserving O / conjugation | ℤ₃ / O ↦ Ō | ℤ₃ / O ↦ Ō |
| V, π₁(V), π₄(V); locking inventory H₀ (rank 0/1/2) | S¹⁵, 0, 0; G₂/SU(3)/SU(2)_long, all π₃-injective, π₄(G₂/H₀) = 0 | identical |
| T1: FS(χ_{3/2}), χ(1), χ(−1); branchings of the 7 | −1, 4, −4; [2,2,1,1,1] / [3,2,2] / [3,3,1] / [7]; control Sym³ mult 1 | identical |
| T2: dim g₂, dim(g₂ ∩ su(4)_L), 2O min defect, quartet mult, SL(2,7) g² = 1 count, FS sum, ν₄ allowed, involution trace, |1344| | 14, 8, 0.7071, 2, 2, 22, {−1, 0}, −1, 1344 | identical (defect 0.7071) |
| T3: π₁(G₂), π₃(G₂), π₄(G₂); controls π₄(S²), π₄(S³), |π₁(SO(3)/T)| | 0, ℤ, 0; ℤ₂, ℤ₂, 24 | identical |
| T4: lifts, order-2 traces, order-4 traces, perm char, χ₆(2A), gap | 24; {−1} (12); {−1, 3} (12); 3; 2; 4 | identical |
| 1344 group (E-A1-6(a)) | NON-SPLIT: 0 complements / 64 lift pairs | identical |
| ℍ screen | H1 EXCLUDED(i,iii) [control]; **H2, H3 ADMISSIBLE-IMPORT**; H4 EXCLUDED(i); H5 EXCLUDED; admissible {H2, H3}; I5 registered; P-ℂ CLOSED-BY-INSTANTIATION | identical (H5 booking differs — §3) |
| Phase 2 (pinned Fano-line texture) | |O_line| = 4π (12.566…), O_nonline = 0 exactly; SO(4) image / SU(2)_long kernel / subdirect / π₀ = 1; loop images {Id, σ_ℍ} (INTERNAL-HOLONOMY-GAUGE); π₁(Orbit) = 0; chiral pair e^{±iα} | identical |

Both legs, without coordination, found the same un-anticipated structural fact — the decoupled real-unit phase u(1)_{ψ₀} (H-A1-1 chat / H-CC-1 CC) — and both recorded it without touching any test.

## 3. The six misses, classified (S9) — all definitional, zero substantive, zero representational

| # | Row | chat | CC | Classification |
|---|---|---|---|---|
| 1–3 | `phase0.sym_int_continuous` equal / `_dim` equal / (chat) in domain | `G2xU1_psi0` / 15 / out of domain | `G2` / 14 / — | **Definitional (pre-declared).** Both legs computed the identical 15-dim stabilizer with the identical identification; the schema domain was frozen before the finding; chat recorded the full stabilizer in the schema keys deliberately out of domain (H-A1-1), CC recorded the memo-pinned 7-sector value and put the full stabilizer in `extras` (CC-DD-2). |
| 4 | `phase1.T2.spinor_2O_automorphism_count` equal | 1 | 0 | **Definitional.** A-2.3's wording ("count of elements with defect 0") includes +Id; its expectation ("expected 0") and its own min-defect domain exclude ±Id. Chat counted literally (H-A1-2), CC over 2O∖{±Id} (CC-DD-1). +Id is the only zero-defect element on either leg (−Id is never an automorphism); min defect 0.7071 both legs. |
| 5–6 | `phase1b.H_screen.H5.i` equal / `.code` equal | fail / `EXCLUDED(i,ii)` | pass / `EXCLUDED(ii,centrality)` | **Definitional.** Whether e₈'s non-centrality (the P-ℂ exclusion of H5; `P_C.e8_central` false, `e8_anticommutes` true on both legs) is booked under criterion (i) (chat, H-A1-2) or under the `centrality` token of the memo §2.4 code strings (CC, CC-DD-3). Same exclusion, same admissible set {H2, H3}. |

Neither checkpoint was edited at any point. **Supplementary run (candidate, not the run of record):** schema v1.1 CANDIDATE r2 `88a92fe2bd17465841f2aacb323e2408` (9,501 B; supersedes the domain-only candidate `69ffc8fb`) + comparator v1.1 CANDIDATE `g_2a_a1_compare_v1_1.py` `7ce866cc83e6a903378bba3a66c9475c` (25,746 B; 22/22 selftest suites — the 15 of v1.0 plus S16 (the run-1 conventions must pass) and S17–S22 (each harmonization must still miss a genuine disagreement); T1 CLEAN) on the two checkpoints of record: **436 checks, 436 PASS, 0 MISS** (`5c8f6a3585e6d938984d994e786f599b`). The three harmonizations are recording-convention rules only, each vacuous-PASS solely under stated witness agreement and otherwise the v1.0 exact rule: **R1.1-1** the `sym_int` rows compare the 7-sector normalization (`G2xU1_psi0`/d ↦ `G2`/d−1; a genuine `G2`/15 vs `G2`/14 still misses); **R1.1-2** the automorphism-count row is vacuous iff both legs' min defect exceeds 10⁻⁶ and both counts lie in {0, 1}; **R1.1-3** the H5 (i)/code rows are vacuous iff both legs agree on `P_C` and each leg's booking is internally consistent. The row count is unchanged (436). **Frozen only on the author's election (§6).**

## 4. Findings of record (R1-machine two-leg; readings R2 conditional; identifications R3)

1. **The ρ₆/Cℓ(6) = ℂ⊗𝕆 factor of the action of record cannot carry the baryon's spinor: ASSIGNMENT-I-FORCED.** Four configuration-independent theorems on Sym_int and V (E-A1-3(a)), each machine-instantiated in exact arithmetic on both legs: **T1** (representation-theoretic) — χ_{3/2} of 2O is quaternionic (FS −1), so a spin-3/2 quartet needs a quaternionic structure the real 7 of G₂ does not supply (even-multiplicity bound ⌊7/4⌋ → 0), and no sl₂-class of g₂ branches the 7 with a 4; **T2** (algebraic) — g₂ ⊂ spin(7)_L meets su(4)_L in exactly su(3) (dim 8), the spinorial 2O ⊂ SU(4)_L contains no non-trivial automorphism of 𝕆 (min defect 0.7071), SL(2,7) has no real-type 4 (ν₄ ∈ {0, −1} from exactly 2 solutions of g² = 1 and the ordinary FS sum 22), every G₂-involution has trace −1 on the 7; **T3** (topological) — π₁(V) = π₄(V) = 0 for V = S¹⁵, π₁(G₂) = 0 (no internal double cover), π₄(G₂/H₀) = 0 for every locking stabilizer, envelope-in-𝕆 killed by π₂(S⁶) = 0; **T4** (the signed-versus-permutation module) — the order-2 signed lifts of the Fano line-stabilizer involutions have trace −1 against the permutation character 3 and χ₆(2A) = 2 (gap 4), so PSL(2,7) ⊂ G₂ acts on the 7 as ρ₇ or 1 ⊕ 3 ⊕ 3̄, never through ρ₆ — the §3.4.7 "wrong module" correction, machine-instantiated.
2. **Sym_int of the action of record is g₂ ⊕ u(1)_{ψ₀}, not "exactly G₂" (H-A1-1 ≡ H-CC-1).** The 15th generator is the phase of the real-unit complex line alone — decoupled because O carries only imaginary indices and the two-body kernel sees only |ψ|² (Q-A1-3: no real-unit-splitting term). The 7-sector continuous symmetry is exactly g₂; the diagonal phase is broken to ℤ₃; conjugation carries O ↦ Ō. No test is affected (an abelian spectator supplies no central −Id on the 7-sector, no spinor SU(4), no binary-polyhedral π₁); F-A1-1 does not fire. The memo's Phase-0 expectation sentence was wrong on this point on both legs' evidence — a corrected expectation, recorded, not a verdict change.
3. **The ℍ factor is an import (I5), and the screen says where it can live.** H2 (the declared M.ONT texture envelope, Q-A1-2) and H3 (the spatial-spinor index; the Dixon ℍ carrying the Lorentz structure) are ADMISSIBLE-IMPORT — H2 with (iv) dynamical, H3 with (iii) conditional and (iv) kinematic; H1 (the in-𝕆 hopfion; role control) EXCLUDED — not tensor-commuting and not topologically protected (π₂(S⁶) = 0); H4 (a Fano-line ℍ_L ⊂ 𝕆) EXCLUDED — not tensor-commuting (its ℍ_L^⊥ module is the 2 ⊕ 2 of the long-root branching, Casimir 3/4); H5 (sedenion doubling) EXCLUDED — not quaternionic and e₈ anticommutes with every imaginary unit (P-ℂ centrality). **F-A1-6 silent: no candidate inside the field content of record passes all four P-ℍ criteria**, so the ℍ factor cannot be instantiated without import. Per E-A1-5(a): I5 — the ℍ-factor / spinor-carrier import — and the §2.50 per-strand spinor-phase import are **one import in two bookkeepings** (registered, not discharged). P-ℂ CLOSED-BY-INSTANTIATION (the complex unit is central on ℝ¹⁶; e₈ is not).
4. **Phase-2 witness on an instantiated core (zero verdict weight; 2-D).** The pinned degree-1 Fano-line texture in (e₁, e₂, e₄): O = 4π·φ_abc on the line L = {1,2,4} and exactly 0 on the non-line L' = {1,2,3} (the §3.4.4 selection rule as an exact zero of the structure constants); the stabilizer in SO(2)_space × Sym_int is a subdirect product with internal image SO(4) = Stab_{G₂}(ℍ_L), kernel SU(2)_long, π₀ = 1; the internal images of the spatial 2π loop over the lifts are {Id, σ_ℍ} — path-dependent, hence INTERNAL-HOLONOMY-GAUGE (both exhibited as exact 8×8 matrices on both legs); **π₁(Orbit) = 0 — the 2π-rotation loop is contractible in the collective-coordinate orbit: no spin sign** from the substrate's internal geometry; the G-INT1 chiral pair transforms ordinarily (e^{±iα}). This is T3's conclusion witnessed on an instantiated core, not a half-integer-spin statement (which is 3-D and carried by T3).
5. **Banked R1 structural (E-A1-6(a)):** the order-1344 signed Fano group is a **non-split** extension 2³·PSL(2,7) (0 complements among the 64 lift pairs of ⟨a, b | a², b³, (ab)⁷, [a,b]⁴⟩; corroborated in print, memo §12 L-A1-3). Only F₂₁ lifts unsigned (21 of 168); all 168 lift signed.
6. **What this does to the programme (E-A1-7(a); R2 conditional on E-A1-1(a) and the adopted homotopy facts).** §2.87.A's sentence "μ_n's spin-3/2 requires Assignment II (or an equivalent derivation relocated to ℂ⊗ℍ)" resolves to its **second clause**: "σ₄|_{2O} = spin-3/2" stands as algebra and is relocated as physics to the ℍ factor (I5); the Gate 2a headline question "is the baryon's spin geometry ρ₆/Cℓ(6)?" is answered **NO at the substrate level**; the *dynamical* clause (M.CW; the I1–I3 ticket) is untouched. The spin-3/2 quartet must be built in the imported ℍ factor (the Sym³(ℂ²) route of §2.87.C P1, whose per-strand carrier is I5); the derived μ_n rides (I5) + (the locking dynamics behind M.CW) + (the FR/η parity of Gate 2b). Nothing here moves μ_n.

**Hypotheses (memo §6, cited HYP-A1-n from here on — see H-A1-6):** HYP-A1-1 (ASSIGNMENT-I-FORCED), HYP-A1-2 (screen admits exactly H2, H3; P-ℂ closes), HYP-A1-3 (non-split 1344), HYP-A1-4 (Phase-2 structure) — **4/4 concordant on both legs**; HYP-A1-5 is a scoping note, not a hypothesis under test (§7).

## 5. Honesty ledger (both legs)

Chat: **H-A1-1** (u(1)_{ψ₀}; the memo's "exactly G₂" corrected; recorded out of the frozen domain), **H-A1-2** (post-lock operationalizations: the H-screen code rule; the identity counted in the automorphism count; `pi0_generic` adopted), **H-A1-3** (self-caught Hermitian-transpose bug before any result), **D-A1-1** (armor-transport collision {9: 3} in one dispatch base64 body; plaintext CLEAN). **New, chat-side, post-CC:** **H-A1-6 — label collision.** The memo §6 hypotheses and the execution report §7 honesty items share the `H-A1-n` namespace (the memo's own text calls the scoping note "H-A1-5"); CC's return uses both senses ("chat H-A1-1" = the honesty item; "H-A1-3 concordant" = the hypothesis). From this memo on the §6 hypotheses are cited **HYP-A1-n**; honesty items keep H-A1-n with 4 and 5 left unassigned so that no honesty label collides with a hypothesis label that the locked memo cites as load-bearing. **H-A1-7 — memo §10 date slip.** The locked memo's housekeeping text states PR #24 "merged September 19, 2026"; the merge commit `0378056` is dated **September 20, 2026 (02:58 −07:00 = 09:58 UTC)**. The fold bracket carries the commit date; the memo is not edited (locked).

CC: **H-CC-1** (u(1)_{ψ₀} found independently; seconds the v1.1 domain widening; election left to the author), **H-CC-2** (lock record A-2.2's parenthetical index expectation "su(3)-sl₂ 1" is a slip — the [3,3,1] class is the principal sl₂ *of su(3)* with embedding index **4** (long-root = 1, short-root 3, principal 28)), **H-CC-3** (self-caught assembler bug before any committed result); **CC-DD-1..3**; no deviations from the binding order of operations; the CC-optional MV-G1 extension not executed.

**H-CC-2 confirmed chat-side (erratum of record for A-2.2):** from the branchings alone, index = Σ n(n²−1)/6 normalized to the long-root class — [2,2,1,1,1] → 1, [3,2,2] → 3, **[3,3,1] → 4**, [7] → 28 (cross-checked by the weight-square sum). Witness-only: no schema key carries these indices (the locking inventory's `sl2_generator_index` = 1 is the long-root generator of each locking, a different quantity, and agrees on both legs); no comparator impact; the lock record is T3-immutable, so the erratum is recorded here and in the fold text.

T1: CLEAN on every artifact, both legs; the two list embeds the only scan exemptions in the dispatch; CC's return needed none.

## 6. S9 election requested

- **(a) Close on run 1 as classified — recommended.** Six definitional misses, zero substantive; every verdict-bearing quantity and every witness at zero deviation; determinism witnessed on both instruments. Freeze schema v1.1 r2 `88a92fe2` + comparator v1.1 `7ce866cc` as the **supplementary run of record: 436 / 0 (`5c8f6a35`)**; no re-emission. (If the author prefers the narrower domain-only candidate `69ffc8fb` with a comparator asserting it, the supplementary run would clear misses #3 only — 436 / 5 — which is why r2 is the candidate presented.)
- **(b) Full cycle (the G-2a-L1 precedent).** Freeze v1.1 r2; both instruments adopt the harmonized conventions as sanctioned deltas (record `G2xU1_psi0`/15 with the 7-sector dim alongside, count over 2O∖{±Id}, one H5 code rule); S9 mini-dispatch; CC re-run blind; run 2 expected 436 / 0. Nothing physical can change — both legs' mathematics already agrees to the last integer.

Either way §4's findings and registers are unchanged.

## 7. Registers and non-claims (for the fold)

Every Phase-0/1/1b check and every Phase-2 number R1-machine two-leg (exact arithmetic; floats only in the A-2.3 defect block and the Phase-2 integral, thresholded). The verdict class R2, conditional on the action of record being the instantiated substrate (E-A1-1(a); the pinned extract `940b0bee`) and on the adopted homotopy facts (cited in both checkpoints); the ℍ-screen outcome, the P-ℂ closure and the I5 unification R2; the H2/H3 identifications R3 (named, not made). **Non-claims (binding, as locked):** no μ_n value, no ratio, no observable, no bridge (M.BRIDGE intact); no locking dynamics (M.CW; I1–I3 untouched); Gate 2a's dynamical clause not closed; no §2.50 closure (unified with I5, not discharged); no retraction of any §2.87 algebra result (E-A1-7(a) — §2.87 filed the physical claim as a non-claim); no statement about the Pin type (the S9 residual unchanged); no statement about G-QUANTA's verdicts; the gauge-paper §7.4 firewall held; **§2.52 Open 3 untouched.** **Successor registered, unopened (HYP-A1-5 / memo §9):** *vacuum selection in the 16-component substrate* — under the reading of record V = S¹⁵ and π₁(V) = 0, so the phase-vortex sector is not topologically protected; whether a G₂-invariant real-unit-splitting term (allowed by symmetry, absent from the action of record) is required for a protected filament sector is a bounded structural question for a successor gate; it bears on the G-QUANTA C1 K₇-vortex row only through its declared configuration space (scoping note; no retraction implied).

## 8. Fold plan (V4.84) — awaiting the author's word

- Title/As-of bump; V4.84 fold-in record ahead of V4.83 (authorization file recorded verbatim; the reverse-splice byte-identical to `40009ec0`).
- **§2.91.R** (Gate G-2a-A1) as a bold-lettered paragraph after §2.91.Q, immediately before the "## J. Multi-Lens Reference and Phase Incommensurability" heading.
- One **Part VI** row (Gate G-2a-A1, after the G-MSCS1 row).
- **Additive brackets (E-A1-7(a)), anchors read from the file and asserted unique:** the §2.87 open-items **"Factor-assignment question"** row → CLOSED (V4.84, §2.91.R): Assignment I forced on the action of record, R2 conditional; the **"Gate 2a — is the baryon's spin geometry ρ₆/Cℓ(6)?"** headline row → answered NO at the substrate level, the spin geometry relocated to the ℍ factor (I5), the dynamical clause unchanged; the §2.87 "What §2.87 does NOT claim" sentence ("σ₄|_{2O} = spin-3/2 … forced") → stands as algebra, relocated as physics; §2.87.A's "Gate 2a, sharpened" paragraph (the G-2a-L1 "Assignment NEUTRAL" annotation) → decided by G-2a-A1; the **M.ONT declaration** line ("texture envelope carrying the rotor band (= spin)") → H2 ADMISSIBLE-IMPORT under P-ℍ, H1 (the envelope in 𝕆 alone) EXCLUDED, the carrier the imported ℍ factor I5; the lock-record A-2.2 erratum (H-CC-2) carried in §2.91.R and the fold record.
- **Housekeeping bracket (memo §10) on §2.91.Q's estate sentence and on the V4.83 fold-in record's "not yet in the repo — a successor PR into gmscs1_gate/estate/" sentence:** PR #24 merged September 20, 2026 (`0378056`, per H-A1-7); the gmscs1 fold-side estate's successor PR number and merge date **to be filled at fold time from the merged PR** — as of this memo `main` is still at `0378056` and `gmscs1_gate/estate/` does not exist, so that PR has not landed; the canonical ledger stays out of the repository (FOLD_LEDGER_2026-06-18).
- One changelog line. Append-only; the §2.52 Open 3 row untouched; no retraction; nothing prior modified.
- **Repository state to settle before the fold record can cite it:** (i) CC has not opened a PR for `claude/new-session-j6430a` (the G-2a-A1 CC leg of record; a fast-forward of `main`) — CC offered to on the author's word; (ii) the gmscs1 fold-side estate successor PR; (iii) the G-2a-A1 chat-side estate (this memo, the v1.1 candidates, the authorization, the fold script) lands later in `g2aa1_gate/estate/` by its own successor PR, per the precedent.

## 9. Estate

Chat: `outputs/g2a_a1/` — draft v1 `ee3ce0e6` (50,251); memo v2 `626aa868` (61,562); lock record `8732c390` (11,279); schema v1.0 `53111e03` (6,405); comparator v1.0 `5afcd589` (20,890); v1.1 candidates: schema r2 `88a92fe2` (9,501; the domain-only `69ffc8fb` superseded), comparator `7ce866cc` (25,746); T1 gate `2026b782` (241) = base `05302210` (143) + stratum `ea3bbbbd` (98), scanner `6b862900` (1,967); extract `940b0bee` (12,912); prechecks `cf36395d` + log `81b55128`; instrument `6360709d` (53,706); checkpoint `8f657a23` (8,247); compare `ba087607`; run log `c3890330`; execution report `3c82ded0` (16,601); dispatch builder `9916ac0a`; dispatch `b2aa9953` (340,070); this memo. CC (`cc/`): return `6034515b` (302,603); instrument `006cb521` (76,216); checkpoint `bbf94221` (8,652); two-leg comparison `5942248e` (66,776) = the chat-side run; CC-vs-CC `73a19a11`; chat-vs-chat re-run `ba087607`; v1.1 supplementary `5c8f6a35`; determinism re-execution: timing-stripped canonical `42a65c26` on both. CC repo: `gifgaf0/gifgaf0.github.io`, branch `claude/new-session-j6430a` (`8323968` → `2be2afc` → `0002154`), `g2aa1_gate/`; no PR yet.
