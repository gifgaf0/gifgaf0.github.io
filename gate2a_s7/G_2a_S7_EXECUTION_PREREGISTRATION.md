# G-2a-S7 EXECUTION PRE-REGISTRATION
**Gate:** G-2a-S7 — the flat crystallographic home (promotion of §2.87.E's R3 bank)
**Date registered:** July 9, 2026. **Base:** V4.55 (candidate). Filed before machine computation; LSF run first (§6); two registration-time hand-sketches disclosed in §5 per Eddington honesty.

## 0. The question
§2.87.E banked (R3) the fact that the Borromean rings are the cone-angle-π singular locus of a **Euclidean** orbifold — E³/Γ for the cube-folding crystallographic group Γ generated by π-rotations about three orthogonal families of skew axes (CHK Ex. 2.32 / Jones Thm 3.4). This gate executes the banked computation: **N(Γ)/Γ = Isom(the flat Borromean orbifold)** — which symmetry classes of the Borromean rings are realized as *static isometries of the flat home*, and by what mechanism. S6 proved the odd coset is intrinsically dynamical in smooth flat ℝ³; the decisive structural question here has the same shape: **does the flat orbifold home realize the odd coset statically — and do lattice translations realize the turn-over classes?**

## 1. Eddington guards
- **The 48 = 48 same-integer trap:** if |N/Γ| = 48 = |ℤ/2 × S₄|, the order match proves nothing. The identification claim requires the explicit realization map Φ_flat: N/Γ → (σ, ε, d)-bookkeeping to be a **faithful homomorphism onto the parity-constrained 48-element group, over the declared strand↔family base** (family direction e_f ↔ ring axis f, the same M.ONT dictionary as §2.87.E).
- **The S6-echo bias:** S6's pyritohedral ceiling must not bias the expectation here in either direction — the orbifold category is different (the singular locus is scaffolding, not obstruction). Both branches (odd coset realized / not realized) banked at equal weight, though the disclosed sketch (§5) points to one.
- No numeric targets; μ_n and the factor-of-4 sealed; §2.50 untouched; §2.52 Open 3 untouched.

## 2. Objects (declared)
Γ = ⟨r₁, r₂, r₃⟩ ⊂ Isom⁺(E³): r₁ = π-rotation about {(t,0,½)}; r₂ = π-rotation about {(½,t,0)}; r₃ = π-rotation about {(0,½,t)}. Equivalently the coset description: point parts V₄ = {I, A₁, A₂, A₃} (Aᵢ = diag with +1 in slot i), vector system a(A₁) = (0,0,1), a(A₂) = (1,0,0), a(A₃) = (0,1,0), lattice L = {v ∈ ℤ³ : all coordinates even or all odd} (body-centered, covolume 4, cell-2 scaling of the ITA convention). Base axes with orientations: ℓ₁ = {(t,0,½)}, +e₁; ℓ₂ = {(½,t,0)}, +e₂; ℓ₃ = {(0,½,t)}, +e₃; strand f ↔ family f.

## 3. Hypotheses (pre-registered, falsifiable)
- **H-A (R1 — Γ is the right group).** (i) The coset description is a group (vector-system closure mod L) and equals ⟨r₁,r₂,r₃⟩ (generation of all four cosets and of all of L by words). (ii) Torsion = π-rotations exactly, forming three mutually orthogonal families of pairwise-skew axes: x-family at (y,z) ∈ ℤ×(ℤ+½), cyclically for y, z. (iii) Γ acts transitively on each family (⇒ the singular locus has exactly 3 circles). (iv) Type identification: proper, point group 222, three genuine-rotation families pairwise skew, I-centering ⇒ **I2₁2₁2₁ (#24)** by the standard characterization; adopt CHK Ex. 2.32 + type-uniqueness ⇒ E³/Γ = the Borromean orbifold, cone angle π. Falsifiers: closure failure; intersecting axes; >1 orbit per family; a missing centering.
- **H-B (R1 — the banked computation).** Compute N = N_{Isom(E³)}(Γ) at the cubic point. Since P_N must normalize V₄-diag, P_N ⊆ O_h (48 signed perms) — finite affine solve: for each B, solve b mod L for the three conjugation congruences. Targets: T_N (pure-translation part), P_N, |N/Γ|, and the space-group type of N; **cross-check against the tabulated Euclidean normalizer of #24** (IT-A 3.5 / Koch–Fischer; Bilbao) by fetch at execution — two-source verification. **Decisive bit B1: is the odd axis-permutation coset realized in N (P_N = O_h) or not (P_N = T_h)?** (Since −I ∈ N is forced by the vector system's 2a_i ∈ L, T_h ⊆ P_N always; the odd coset is the open question.)
- **H-C (R1 — the realization map).** Compute Φ_flat: N/Γ → (σ, ε, d): σ = the family permutation |B|; ε_g = the direction character on the singular circle, via the γ-correction test (find γ ∈ Γ returning the image axis to the base axis of its family; ε = the sign of the e_g-component of (A_γ B)e_f; well-defined because axis stabilizers in Γ preserve direction — families are pairwise skew, so no perpendicular intersecting π-axes exist); d = det B. Claims: (i) Φ_flat is a well-defined homomorphism; (ii) **faithful** (48 distinct images if |N/Γ| = 48); (iii) image = the full parity-constrained bookkeeping group {(σ,ε,d) : Πε = d·sgn(σ)} — i.e. **Isom(flat home) ≅ ℤ/2 × S₄ realizing every symmetry class of the Borromean rings, the odd coset statically included**; (iv) **the translation dictionary — decisive bit B2:** the pure-translation subgroup ℤ³/L ≅ (ℤ/2)² of N/Γ maps to **{identity} ∪ {the three turn-over classes}, with the strand-indexed alignment: translation by e_f ↦ the turn-over fixing strand f (σ = id, ε_f = +1, ε_{g≠f} = −1)**. Falsifiers: kernel ≠ 1; image ≠ the parity-constrained group; any translation mapping to σ ≠ id, to ε = +++ (nontrivially), or with misaligned indexing.
- **H-D (R2 — verdict synthesis; M.REL).** If H-B/H-C pass: refine §2.87.E's grading — "the odd coset is intrinsically dynamical in flat space" sharpens to "…in flat **smooth** space; in the flat **orbifold** home the odd coset is static, at the cost of the orbifold import (the cone-π singular scaffolding, undeclared physically)." Cross-geometry table: smooth flat ℝ³ — proper ceiling A₄ (T), with impropers T_h; flat orbifold — [this gate]; round S³ — full 48. M.REL axes: metric — flat both sides here; the new axis is **structure/ontology** (cone singularities as scaffolding; any physical reading undeclared, R3). No dynamics, no observable, no dimensionful constant — M.CW/M.BRIDGE intact.
- **R3 notes (logged, uninterpreted):** (a) if B2 lands: *turn-over = half-lattice translation in the flat home* — banked adjacent to §2.50 thinking, no import claimed; (b) the box moduli (a:b:c) of the flat structure, cubic = maximally symmetric point; (c) the torsion-free sister #19 (Hantzsche–Wendt); (d) universality: every closed orientable 3-manifold is a branched cover of S³ over the Borromean rings with indices 1, 2, 4 (Hilden–Lozano–Montesinos–Whitten) — the Borromean orbifold family is universal.

## 4. Register ceilings and scope
All identification claims live at the bookkeeping level over the declared base; composition with §2.87.D–E gives the full-group statements. No physical import for the singular scaffolding. §2.52 Open 3 untouched.

## 5. Registration-time sketches (disclosed)
Two hand computations were performed while scoping this registration, before filing; both are superseded by the machine legs and their falsifiers remain live: (i) solving the conjugation congruences for one proper-odd candidate B: (x,y,z) ↦ (y,x,−z) suggested b = (½,½,½) normalizes Γ — pointing to P_N = O_h and |N/Γ| = 48; (ii) the γ-correction test for the translation e₁ suggested ε = (+,−,−) — pointing to the B2 turn-over dictionary with strand-indexed alignment. These sketches set the registered expectations; the machine computation, the CC leg, and the table cross-check decide.

## 6. LSF record (run before this filing)
- CHK Ex. 2.32 / Jones Thm 3.4: the Borromean rings labelled 2 = Euclidean orbifold (cube-folding). Adopted.
- IT-A ch. 3.5 (Koch–Fischer–Müller): Euclidean normalizers of space groups are tabulated; Bilbao Symmetry Database hosts them. The H-B cross-check target.
- Cantarella–Fu–Kusner–Sullivan–Wrinkle (Gehring link problem): symmetric Borromean configurations carry pyritohedral 3*2 ≅ ±A₄ symmetry in O(3) — external corroboration of §2.87.E H-B; adopted for context.
- CDHT "On three-dimensional space groups" (fibrifold framework): the space-group ↔ orbifold naming; adopted.
- Hilden–Lozano–Montesinos–Whitten: Borromean branched-cover universality — R3 bank item (d).
- **No published computation found of Isom(flat Borromean orbifold) expressed in link-symmetry (σ, ε, d) terms, nor of the translation↔turn-over dictionary: novel-in-assembly; every ingredient standard and attributed.**

## 7. Execution plan
**Chat leg:** exact Fraction arithmetic, stdlib only — H-A closure/generation/torsion/orbit checks; H-B affine solve over B ∈ O_h, b ∈ (½ℤ)³ mod L, plus the Bilbao/IT-A fetch attempt; H-C full Φ_flat table with homomorphism, faithfulness, image, and B2 checks. **CC leg (zero shared machinery):** own implementation from the ITA standard setting of #24 (cell-1 convention, different origin), or an independent solve; the table cross-check independently. **Audit → fold V4.56:** §2.87.F + Part VI row + R3-bank discharge pointer in §2.87.E + record/changelog.

*Filed July 9, 2026. Decisive bits: B1 (odd coset in N?) and B2 (translations ↦ turn-overs, strand-aligned?). Both branches of each banked at equal weight; the §5 sketches are disclosed expectations, not results.*
