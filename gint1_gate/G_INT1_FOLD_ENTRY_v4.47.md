# G-INT1 — Fold-ready entry (PREPARED; PENDING author authorization)

**Status:** SQT-cleared (two-leg agree; three corrections + one precision flag all
incorporated). **Not yet folded.** Per SQT: "authorize when you want it folded."
**Constraint:** canonical V4.46/V4.47 is **not in this repository** — the byte-md5 splice
below is specified as an *instruction set* for whoever holds the canonical copy; this file
is the source of record until incorporated.

---

## Ledger home (SQT-suggested)
- **§2.88.D.2** under the **G-ζ1 umbrella**, cross-referencing **§2.88.D.1** (density
  channel), so density (D.1) and internal (D.2) read as one investigation with two results
  and the M.BRIDGE "two channels, two distinct imports" sits legible in one place.
  *Confirm numbering against the live ledger (SQT reconstructed from search; may run ahead
  of the project copy).*
- **Paper-side ladder extension → Paper II §3.4** (the §3.4.4/§3.4.5 selection-rule ladder
  extended to the fluctuation spectrum). **Not** ledger §3.x (that is the retraction log) —
  same call as the V4.26 §3.4-SYM patch.

## Register tags (exactly as disposed)
- **R1:** (i) two-body internal **gaplessness theorem** (ψ₀ = exact internal zero mode,
  L_⊥ψ₀ = the GP equation = 0); (ii) **S_Fano binary selectivity** (oriented term lifts an
  internal mode iff the winding triple is a Fano line); (iii) **multiplet 1 ⊕ 3 ⊕ 3̄**
  under F₂₁ *(prior-art note: standard G₂/F₂₁ representation theory — used to classify, not
  claimed new)*; (iv) **2-chiral count + ± chirality** (forced by total antisymmetry of φ
  on a 3-element line → {0, ±iσ}; profile- and λ-independent). **Two-leg verified.**
- **class-(b):** the internal gap magnitude **σ = ‖φ|_line‖ · λ · (core-profile integral)**,
  ‖φ|_line‖ = √3 (norm of φ on a Fano line — *not* a representation number, *not* the p6m
  lattice √3). **λ is the located import for the INTERNAL channel.**
- **R2 verdict:** **STRUCTURAL-ONLY** — internal channel **eliminated as a breach route**;
  no geometry-protected pure dynamical number (GAPPED-SCALE-FREE does not fire).

## Cluster status (precision-corrected — do not launder)
- G-INT1 closes off the **internal** channel as a breach route.
- **§2.53/§2.64 remain bottomed on the density-sector roton / healing-length import**,
  which is the **live frontier** — **angle-3 (the ξ_vac/a-forcing test) is UNEXECUTED**,
  and G-ζ1 only *used* (did not derive) the roton profile. Do not let "G-ζ1 used the
  profile" imply that import is located.
- **§2.52 Open 3 frozen** and untouched.
- Net: three gates; internal channel now closed off; **density import still the open
  target**; two distinct imports identified (density roton/healing-length profile, open; λ,
  internal-channel, located here).

## See-also (cross-ref, not a claim)
3/3̄ split = the QR/QNR split (the i√7 in the F₂₁ character is the Gauss sum of 7th roots
over residue vs non-residue cosets) → **§2.75/§2.76**, **§3.4.6** sign(φ)↔QR/QNR map.

## Splice discipline (non-negotiable; for the canonical-holder)
1. **§2.52 Open 3 row asserted BYTE-IDENTICAL** pre/post splice (frozen).
2. **md5 byte-reconstruction against V4.46** (cannot be performed here — V4.46 not in
   repo; run against the canonical copy).
3. **Append-only**; no prior body content modified.
4. **Intra-line host edits ENUMERATED:** the As-of/version bump (V4.46 → V4.47) and any
   forward-pointer added onto **§2.88.D.1** and **§3.4.5** — list each, change nothing else.

## Provenance (two-leg)
- First leg: `gint1_execute.py` + `octonion_fano.py` (commit `41c6bb9`).
- Second leg (independent, from-scratch, not using `gz1_core`): `verify_gint1_secondleg.py`
  (SQT) — reproduces φ-on-7-lines, F₂₁ order 21, 1⊕3⊕3̄ character, line eigs {0, ±i√3},
  non-line zero.
- Report: `G_INT1_EXECUTION_REPORT.md`; verdict: `gint1_verdict.json`; this entry.

**Authorize to incorporate into canonical V4.47.**
