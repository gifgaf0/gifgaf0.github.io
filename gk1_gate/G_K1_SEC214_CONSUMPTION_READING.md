# G-κ1 — The §2.14-Consumption Reading (chat-side audit; the "seal the container" step)

**Date:** July 5, 2026
**Mode:** Pure ledger audit (V4.51 canonical §2.14 + the operational v3 calculator handoff).
No compute; the two derived thresholds below are locked from already-run Q2 numbers,
pre-registered BEFORE any roton-kernel evaluation exists (Eddington guard).
**Directive check:** the author proposed the conclusion in advance (L geometric; screening
outside the exponent as binding energy). Auditor's finding: **the text supports it** — on
four independent grounds, including one falsification clause canon already carries — with
one honest addition: sealing the container *locates a new import at its boundary*.

---

## Verdict: L in §2.14 is STRICTLY the geometric length. Four independent confirmations.

1. **The formula's own structure.** m = m₀·(A/Z_f)·exp(L/(Φ·r_eff(L))) with
   **r_eff(L) = 1 + ln(1 + L/ξ_vac)** — the denominator is a *declared dimensionless
   function of L itself*, not the physical healing length. Nothing in the exponent is
   hydrodynamic; L enters as a pure number.
2. **The electron derivation (§2.14's own R2 result).** L_e = 2π is the §2.50
   first-stable-closure — a *lattice closure count* (2π-B), forced by topology "independent
   of any calibration choice," derived by structural algebra. A count, not an energy.
3. **The proton entry carries a falsification clause in geometric terms.** Canon: "Theorem 1
   inverts to predict the **ideal Borromean ropelength: L_B = 60.194** (within Ashton bounds
   [58.006, 62.0])... If (1) returns a value outside the Ashton interval, the Borromean
   topological assignment for the proton is falsified." L_B is the *ideal (tight)
   ropelength*, checked against the rigorous tight-link bounds (CKS 58.006 as the floor) —
   geometric, definitionally and falsifiably. (The "fm" gloss is a unit convention; the
   quantity consumed and bounded is the dimensionless ropelength.)
4. **Canon has already once rejected a physical-mechanism insertion into the exponent.**
   §2.14's M.SE note: the 4π/Φ (spinor double-cover) hypothesis "felt physical-mechanism-ish
   and was numerically convenient," but was falsified because "the actual derivation chain
   runs through §2.50 + §2.50.A" — structural, not mechanistic. **The identical discipline
   now blocks feeding hydrodynamic tension into the exponent.** The container was built
   sealed; this reading re-applies its own precedent.

Corroborating: no hydrodynamic energy scale is declared anywhere in canon — zero hits for
binding/vortex/flow/hydrodynamic energy or any ρΓ² scale. The exponent has no energy
channel to couple to, and canon never claimed one.

## Consequence 1 — the exponential-amplification catastrophe does not occur

The catastrophe required L_exponent = energy-equivalent length. Canon never makes that
identification. Q2's tension structure (dispersion ~10%, cross-class shifts ~tens of %)
**cannot enter the exponent** — the exponent consumes ideal geometric L only. The mass
ratios are safe from channel (iii) *through this route*, by construction.

## Consequence 2 — sealing the container locates one import at its boundary

Physical mass is energy. The hydrodynamic ledger E_hydro = ∫T(s)ds is a real energy of the
configuration with genuine knot-class-dependent structure (Q2). Since it cannot live in the
exponent, exactly one of the following holds, and canon currently declares none of them:

- (a) E_hydro's absolute scale (in mass units) is small — the exp-channel dominates;
- (b) E_hydro is core-dominated (C large), making it ∝ geometric L and ratio-neutral;
- (c) E_hydro couples to observed mass in some declared way (e.g., as the composite's
  binding-energy ledger — the author's framing) with a computable, bounded effect.

**The located import: the E_hydro ↔ mass coupling scale.** This is the M.BRIDGE pattern
exactly — the container is internally sealed, and the bridge to physical energy carries the
undeclared import. The author's "binding energy of the composite object" is the natural
physical home for the screening result (the tangle's flow energy is *lower* than the sum of
isolated components — a binding energy in the standard sense); its scale is the import.

## Pre-registered thresholds (locked NOW, before any roton-kernel C exists)

If route (b) is tested — core dominance — the Q2 numbers already fix the comparison values.
With std(ln R_cut) = 0.192 and cross-class ΔT ≈ 1.0 (headline case):

- internal dispersion of E_hydro/L under 3% requires **C ≳ 5.3**;
- cross-class (Borromean vs unknot) tension shift under 3% requires **C ≳ 32**.

These are locked pre-computation: when CC evaluates C[K_roton], the comparison is against
these numbers as registered here, not chosen after the fact. (For calibration: C_GP = 0.38 —
route (b) requires the roton kernel to be *radically* more core-dominated than GP.)

## Banked observation (R3): the r_eff(L) log-echo

The exponent's own denominator, 1 + ln(1 + L/ξ_vac), has the same functional shape as a
flow-energy log cutoff (ln of a length over ξ). Possibly coincidence; possibly where the
*universal, knot-independent* part of the flow physics already lives inside the geometric
formula — which would leave only the knot-*shape*-dependent residual (dispersion +
screening) outside. Banked as an observation, not developed (do not promote without a
derivation path).

## Re-scoping of G-κ1's verdict (for the eventual fold; no arm forced)

The pre-registered arms assumed δ threatens the mass through one container. The audit shows
a **hybrid outcome**, to be folded as such: **D1 = (a)** — the curvature/screening coupling
law carries **no new dimensionless import** (Q1a prior art + Q1b demonstration); **the mass
exponent is SEALED** against it definitionally (this reading; ARM-R for the container);
**D2 large** — but in the E_hydro ledger, not the exponent (the ARM-N magnitude lands
there); and **one new located import** — the E_hydro↔mass coupling scale (the honest price
of the seal). Cleaner than any single arm; the fold should say exactly this.

## CC handoff (now unblocked per the directive's sequencing)

The container is sealed; the handoff proceeds with sharpened targets:
1. **CKS-exact Q2** (replaces the golden-ellipse stand-in): encode the tight-Borromean
   piecewise geometry; rerun the tension pipeline; expect reduced internal dispersion
   (tightness equalizes constraints), persistent screening shift.
2. **C[K_roton]** via the Berloff–Roberts nonlocal method — compared, on arrival, against
   the pre-registered thresholds above (5.3 / 32), not against anything chosen later.
3. Both feed the G-κ1 fold, which states the hybrid verdict + the located import.

§2.52 untouched. Nothing folded this step.
