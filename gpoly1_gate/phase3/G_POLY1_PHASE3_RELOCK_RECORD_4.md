# G-POLY1 PHASE 3 — RE-LOCK RECORD 4 (Addendum P3-A4; Election E3-6(a))
**Date:** August 5, 2026. **Base:** V4.75 `0e923ab7`. **FROZEN:** `staging_memo_G_POLY1_phase3_ADDENDUM_4.md` md5 `659454eda11c969ce7d6736c6f1e1063` (5,481 B), byte-identical to the amended draft (supersedes draft 7ddb6cc2; the §2(ii) glue-class parity item from the CC pre-freeze review incorporated with its pinned expectation). Chain: `e16a7890` + `ba75b113` (`7f4b53ad`) + `ac87bc92` (`b1150c94`) + `257b81bf` (`6a7069b0`) + P3-A4 (this record). Author word, August 5, 2026: *"1. freeze 2. A"* — P3-A4 FROZEN; **election E3-6(a): frozen-ASCII semantics stand; the conformed chat v5 runs read #4; C-P3-1..4 compares like-for-like; W_∪ banked with the A-3 reading flagged CONSERVATIVE; zero additional reads on either leg; superscript adoption named as an optional post-fold successor micro-gate.**

**Binding for v5 (per P3-A4 §2):** frozen-token conformance (the P3-A1 §2(b) list `{ceiling, bound, margin, criterion}` only); identifier/sign guard with glue class `[A-Za-z0-9_.-]` and strict positivity on parsed lengths/frequencies (rejection → fall-through/inheritance); catch-all masked abort (no exception class escapes unmasked or checkpoint-less); independently constructed adversarial synthetics (identifier-glued digit; negative-lookalike positivity rejection; masked-abort mechanics). **H-18** stands. **Read #4 gates:** this record + v5 all-green + the returned `poly1_phase3_cc.json` PRESENT AND HASHED in the chat workspace against `2064bd7b` (in-repo verification by CC at commit `39418ab` acknowledged; workspace placement still outstanding). Elections E3-1..E3-5 unchanged. CC action under E3-6(a): none.
