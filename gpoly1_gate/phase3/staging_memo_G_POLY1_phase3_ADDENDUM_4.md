# STAGING MEMO ADDENDUM P3-A4 — Conformance Repairs, Lexer Guards, Catch-All Masking, Election E3-6
**Date:** August 5, 2026. **Amends:** the chain `e16a7890` + `ba75b113` (`7f4b53ad`) + `ac87bc92` (`b1150c94`) + `257b81bf` (`6a7069b0`) under §6(b). **Status: DRAFT — chat read #4 requires the author's re-lock word on this addendum AND the returned CC checkpoint hashed in the chat workspace.**

## 1. Event record — H-18 (chat v4 defects, disclosed by the blind leg pre-read-#4)
The CC leg (blind, clean run, verdict P-2, checkpoint declared `2064bd7b`) disclosed two chat-v4 defects before chat read #4 was spent: **(a)** the CEIL stated-path regex lacks an identifier guard — a digit glued to an identifier token (hyphenated catalog style) can bind as a negative stated path, driving `att_edge` complex and crashing mid-read with an uncaught TypeError instead of a masked X-1 (no checkpoint; a further exposure H-item; a re-lock anyway); **(b)** the chat DLM adjacency keywords `coefficients`/`set` were implementation INVENTIONS beyond the frozen P3-A1 §2(b) token list `{ceiling, bound, margin, criterion}` — a conformance deviation guaranteed to produce C-P3-4 span divergence. Both accepted as **H-18**. Cross-leg DISCLOSURE of defect classes is admissible (the H-7 precedent); cross-leg CODE is not (§4 below). CC-side review of this DRAFT (pre-freeze) confirmed the §1 record's accuracy against the CC report, verified both returned checkpoints in-repo against their declared hashes, and contributed the §2(ii) glue-class parity item — spec-level input only; no code or synthetics transferred; CC correctly holds the draft out of the repo until frozen (byte-identity is the ledger).

## 2. Chat-v4 repairs (v5; values-blind, from the frozen texts + the disclosed defect classes)
**(i) Frozen-token conformance:** the "first quantity adjacent" primary step uses exactly the frozen P3-A1 §2(b) list `{ceiling, bound, margin, criterion}`, per class applied to its own row text; the invented tokens are removed. **(ii) Identifier/sign guard (all stated-quantity lexing — paths, frequencies, margins, bounds):** a quantity candidate is rejected when its leading character is glued to an identifier character (`[A-Za-z0-9_.-]` immediately preceding the sign or first digit — the period included for strict cross-leg lexer parity per the CC-side pre-freeze review; a `.`-glued digit run is a section/version/identifier fragment, not a stated quantity), and any parsed length or frequency must be strictly positive — rejected candidates fall through to the next candidate or to the pre-registered inheritance. **Pinned pre-registered expectation (CC scan, disclosure-level):** on the sealed file this parity item changes no reading; any C-P3-4 divergence attributable to it would itself be an S9 surprise. **(iii) Catch-all masked abort:** the entire post-open read stage is wrapped so that ANY exception — RowDefect or otherwise — exits as a masked X-1 checkpoint (exception text passed through rb_mask; on masking self-scan failure, label-only); no mid-read failure can escape unmasked or checkpoint-less. **(iv) C-SYN-X extension (independently constructed; no CC code or synthetics imported):** an adversarial synthetic with an identifier-embedded glued digit adjacent to a genuine stated path; a negative-lookalike stated-quantity synthetic exercising the positivity rejection + inheritance; a forced mid-read-crash synthetic verifying the catch-all wrapper emits a masked X-1 checkpoint. All prior gates remain.

## 3. Election E3-6 — superscript semantics (required at re-lock)
The blind leg flagged three frozen-semantics readings for adjudication (A-2 percent-token bind; A-3 superscript-detached mantissa; A-4 margin from a power form); none changes the P-2 class; A-3's is magnitude-relevant (correction can only TIGHTEN the window, never empty it). **(a) Frozen-ASCII stands [recommended]:** the conformed chat v5 runs read #4; C-P3-1..4 compares like-for-like under identical frozen semantics; W_∪ banked with the A-3 reading flagged CONSERVATIVE; zero additional reads; superscript adoption named as an optional post-fold successor micro-gate. **(b) Adopt superscript parsing now:** new semantics addendum; BOTH legs re-run (CC read #2 + chat read #4; full re-lock chain); the tightened window banked. **(c) Hybrid:** frozen comparison first; the author's adjudicated A-3 value then enters as a signed author-arm annotation, no re-reads.

## 4. Leg-independence rule (binding)
CC's offer to draft the chat patch is declined for the chat instrument: importing the blind leg's guard code or synthetics would make the C-P3-4 extraction-reading comparison partially self-referential. The chat v5 is implemented independently from THIS addendum's text; CC's own drafts may live in the repo as documentation only. Disclosure ≠ code.

## 5. Read accounting and unchanged discipline
Chat read #4 runs only after: this addendum FROZEN + author word; the returned `poly1_phase3_cc.json` present and hashed in the chat workspace (declared `2064bd7b`; verified in-repo by CC at commit `39418ab`, awaiting workspace placement); v5 built + ALL gates green. CC requires no further read under E3-6(a). The G_HS obligation is two-leg CLOSED (CC confirmation, checkpoint declared `96951e65` — same return-and-hash request). Elections E3-1..E3-5 unchanged; T1 wall, sanctioned carriers, single-read rule, M.CW, non-goals, §2.52 Open 3: unchanged.
