# G-S2C1-W — LOCK RECORD ADDENDUM 2 (CC return of record; S9-R1 disposition; identity-anchor re-pin; run 3; closure — STAGED, pending the author's word)

**Date:** September 9, 2026. **Amends:** lock record `5f963ed8…` §7, Addendum 1 `12580917…` §4, closure log `376bfad8…` §5, closure-log Addendum 1 `510b9fe7…` §6–7. **Memo** `1ebb6a82…` untouched. **Elections E-W-1..7 unchanged.**

## 1. CC return of record (canonical emission under dispatch `7ca7e9e9937d2f3c12f0521bce859e37`, 115,183 B)
Branch `claude/new-session-q8iuz5`; checkpoint commit `4184c2ab0b4177024725437f269f32b718076ad3`; report + manifest commit `208902e0935d6778eee329a9e1b839fb41f28229`. Fetched by commit SHA (no branch-name resolution) and verified byte-exact: **`g_s2c1w_phase3_cc.json` `6dde4ae6ffb944d94b138985cd7cf6cd` (5,488 B)**; `G_S2C1_W_CC_REPORT.md` `835c478f6997fa85065e92303775271c` (13,848 B); `RETURN_MANIFEST.md5` `c827f1d60b6d5b3b78423e6e2e55d7e4` (327 B) — **5/5**: mapper `g_s2c1w_mapper_cc.py` `cd9c21dcf213a08820a668f401b8e58b` (33,006 B), scanner `t1_scan_cc.py` `6fd00b1b…` (3,167 B), pre-read results `dc707df5…` (647 B), checkpoint, report. CC's chain as reported: dispatch verified ALONE (P-4.c clean), 13/13 embeds byte-exact, machinery asserted in-repo at `dd813646` (MANIFEST 13/13, byte-identical to embed #11), T1 base ∪ A1 = 37 patterns 0 hits (independent scanner), F-W-PIN 46/46 at zero deviation, 24/24 pre-read suites, single spent read (invocation 1 aborted masked CENSUS pre-parse — read NOT spent; CC-DD-3 backtick unwrap on structure-only evidence), per-arm + union serialized and hashed BEFORE the comparison, embed #8 (chat instrument) never decoded.

## 2. S9-R1 disposition — path (a) EXECUTED by the CC leg
The re-emission under the dispatch is exactly closure-log Addendum 1 §7 path (a). The external "CC checkpoint" declared `97f26c04…` never reached the workspace; the text supplied in its name (`722f0f1d…`) failed the independence witness (S9 run 2; D-W-11/12/13) and is SUPERSEDED — it plays no further role. The return of §1 is the CC leg of record.

## 3. Identity-anchor re-pin — **AUTHOR'S WORD REQUIRED**
Addendum 1 §4 pinned the CC identity anchor to `97f26c04…`; the frozen comparators v1.0 `007688e5` and v1.1 `f27a008f` carry that pin and therefore exit 3 (OUTSTANDING) against the return of record, by design (the dispatch §2 clause: disposition of the pin is yours, not an instrument edit). Proposed disposition: **re-pin to the return manifest, `6dde4ae6ffb944d94b138985cd7cf6cd`.** Implemented as `g_s2c1w_compare_v1_1p.py` **`55e6977e801e73df2b3d0b15fe17d7bf`** (7,614 B) = v1.1 logic byte-identical except the pin constant and two label strings (unified diff of three lines, on file). v1.0 and v1.1 remain on the record untouched. **Run 3 of record = v1.1p on chat `af7516c519048ad631f1a867f5f0822d` vs CC `6dde4ae6…`; it has been executed as a preview and becomes the run of record on your word.**

## 4. Run 3 (v1.1p) — 105 items, 19 misses, ZERO on every verdict-bearing quantity
**Zero deviation (PASS):** all four per-arm `W_upper_m` (exact to 1e-6, in fact bit-identical), the pinned G-POLY1 edges, every `disp` edge of record (Born) and dressed edge, kd, Δ₄, `dressing_sensitive`, `voided`, `fail_L`, the arm classes (P-2′ ×4), the union (0, 2.1213132100130068] on `hex:step`, the intersection, the comparison class **INERT**, gate-level `oom_robust` **true** on both legs, reinstatement NONE, sealed md5 + bytes, the lock block (memo/lock/T1/A1/pinned/sealed), anchors B / k / q / row_md5, T1 0 hits both legs (C-W-6), **C-W-7 independence witness PASS on all four arms** (CC F_L = chat F_L × 1.034860 exactly — the CC a₂ column used, E-W-3(a) honored), C-W-0 instrument provenance (real md5 = the manifest's mapper hash).
**Misses — 15 representational + 4 definitional:**
- Representational (15): `sealed/census` structure (chat `{disp:1}` vs CC `{rows, per_class, fields_per_row}`); anchor index base (chat 1 / CC 0) on `anchors/idx` and the four `disp/anchor` fields; governing label string (`GPOLY1-pinned` vs `pinned_gpoly1`, four arms + the comparison dict); `reinstatement` string (long form vs `NONE`); `self_tests` and `instrument` keys absent on CC (CC files its 24 suites in `g_s2c1w_cc_preread_results.json` `dc707df5…`); `cc/pre_comparison_md5` — CC's hash covers its own sealed/anchors/per-arm/robustness/union block, the v1.1 canonical (per_arm+union) was defined AFTER the dispatch shipped schema v1.0 — a chat-side late definition, not a CC defect; CC's comparison-last evidence is present as its own hash `279bb25f…`.
- Definitional (4): per-arm `oom_robust` chat `true` vs CC `false`. **H-W-6 (chat-side, conceded):** memo §3.5 says the arm CLASS must be unchanged under ×10 / ×0.1; the chat v6.1 flag tested the governing EDGE. Under the ×10 relaxation the `disp` edges leave the envelope on every arm and the class flips P-2′ → VOID-DISP (window invariant; a VOID only widens). Reconciliation `H_W_6_reconciliation.json` **`1f8451e817149107bf6d9e9656cf4d4e`** recomputes the flag under the memo letter from the chat checkpoint's own serialized OOM records (no sealed access, no re-read): **false on all four arms = CC.** Gate-level class INERT under base, ×10, ×0.1 on both legs. The chat checkpoint of record is not edited; the reconciliation is the disposition.
S9 cycle: representational misses disclosed, the definitional miss conceded and reconciled — **no re-run of either leg.**

## 5. CC disclosures acknowledged (report `835c478f`)
CC-DD-3 / H-CC-2: the sealed serialization wraps f0 in inline-code backticks; the chat lexer strips them on f0 as well (memo §3.3 field map; both legs read the declared census) — no divergence of substance. H-CC-1: a CC pre-read synthetic numerically equal to the sealed budget, chosen blind, verdict-inert, disclosed not re-cut (the H-W-1 discipline; the chat synthetics {0.05, 0.5, 1e-4, 2.5e-3, 1e-3} are disjoint). CC's OOM nuance is the rule-faithful reading adopted in §4.

## 6. Closure declaration — CONDITIONAL on the §3 word
**On your word: Gate G-S2C1-W CLOSED two-leg — class INERT, OOM-ROBUST: W_∪′ = W_∪ = (0, 2.1213132100130068 m] (governing arm hex:step); the single sealed `disp` budget non-binding on the domain scale by ~5 orders (edges 1.5–2.2×10⁵ m, inside the Born envelope); lattice floor ~10⁻⁸¹ under the declared chain (VOID-TRIVIAL, as registered pre-data); FAIL-L never fired; W_∪′ BANKED ALONGSIDE the suspended W_∪ (E-W-7(a)); NO reinstatement; NO observable; NO bridge; NO channel-speed statement; NO μ_n; §2.52 Open 3 untouched.** Registers: R1-machine two-leg for every number; the INERT reading R2 conditional on E-W-1..3, S-W-1, E-P2-1 (a), G-POLY1's E3 elections and the Born/SOA order; the polycrystal postulate remains R3.

## 7. Proposed standing amendment — **P-5 (your election; NOT folded by this addendum)**
From H-W-5: *a frozen comparator must carry an explicit independence witness for every locked election that forces an inter-leg difference (here E-W-3(a) → F_L ratio); bit-identity where a difference is mandated is a MISS, never a PASS.* Adopt / amend / decline.

## 8. Fold
V4.81 candidate staged by `foldin_v4_81_gs2c1w.py` from V4.80 canonical `a28e9b40…` (seven additive edits; reverse-splice byte-verified; §2.52 Open 3 row byte-identical); candidate hash reported in the chat return. Fold authorization is a separate word; the fold record cites this addendum by hash.
